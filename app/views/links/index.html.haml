.menubar
	%ul.nav
		%li
			%span.menuItem.title= "Moc"
		- if user_signed_in?
			%li
				= link_to("link") do
					%span.menuItem= "Links"
			%li
				= link_to("feed") do
					%span.menuItem= "Feeds"
			
			%li.right
				= link_to new_link_path do
					%span.menuItem= "New Link"
			
			%li.right
				= link_to("logout", :method => "delete") do
					%span.menuItem= "Logout"
			%li.right
				%a{:href => "me"}
					%span.menuItem= "Profile"
				
		- else
			%li.right
				%a{:href => "register"}
					%span.menuItem= "Register"
			%li.right
				%a{:href => "login"}
					%span.menuItem= "Login"

.container
	- @links.each do |link|
		- if current_member.email == link.owner
			%section
				%header
					%h1=link.title
				%article
					%p=link.content